# DominatingSet-Kernel

This repository contains code and data for the paper:
Kernelization as Compression: Dominating Set as a Case Study in Parameterized Preprocessing

It includes:
- Python implementations of kernelization rules (R1–R4)
- Metrics (DCR, NID)
- Experimental driver that generates data.csv
- Scripts to summarize and analyze results

## Requirements

- Python 3.8+
- networkx

## Installation

```bash
pip install networkx pandas matplotlib
